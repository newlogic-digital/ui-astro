---
import type { HTMLTag, Polymorphic } from 'astro/types'

type Props<Tag extends HTMLTag = 'details'> = Polymorphic<{ as: Tag }>

const { as: Tag = 'details', class: className, ...props } = Astro.props;
---

<Tag class:list={['x-details', className]} {...props}>
  <slot />
</Tag>
