image: newlogic/deploy:lithium-php8.5

build:
  stage: build
  artifacts:
    paths:
      - public/
    expire_in: 1 week
  cache:
    paths:
      - node_modules/

  before_script:
    - npm ci

  script:
    - npm run lint
    - npm run check
    - npm run build
